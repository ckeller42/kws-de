# Sphinx doc toolchain. Build:
#   .venv/bin/python -m sphinx -b html docs docs/_build/html
# Requires node >= 20 at build time (sphinx-likec4 runs the pinned LikeC4 CLI).
sphinx>=7,<8
sphinx-needs>=5,<6
sphinxcontrib-mermaid>=1,<2   # mermaid diagrams (.. mermaid:: + ```mermaid fences)
myst-parser>=2,<5             # Markdown docs on the Sphinx site
furo>=2024.1                  # theme (dark-mode)
sphinx-likec4 @ git+https://github.com/ckeller42/sphinx-likec4   # .. likec4-view:: <id>
